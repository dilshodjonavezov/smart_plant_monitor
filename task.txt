Создай локальный проект, который эмулирует IoT-систему контроля влажности почвы и автополива.
Полное техническое задание:

---

# ✅ **Техническое задание для Claude Code — Локальная эмуляция IoT Soil Moisture Monitoring System**

Создай полностью локальный проект, который эмулирует работу IoT-системы контроля и автополива растений.

---

# 🔥 **Цель**

Смоделировать локальную систему «датчик влажности → WebSocket → красивая realtime-страница» на фейковых данных.

Позже настоящие данные будут поступать от Arduino/контроллера.

---

# 🏗 **Что нужно построить**

## **1. Локальный WebSocket сервер (Node.js)**

Создай папку `/server`.

Сделай WebSocket-сервер, который каждые 2 секунды отправляет на фронтенд:

```json
{
  "timestamp": "...",
  "moisture": 0-100,
  "isWatering": true/false,
  "wateringEvents": [
    { "start": "...", "end": "...", "durationSec": 12 }
  ]
}
```

### **Логика эмуляции влажности:**

* Базовый уровень влажности → случайно изменяется в пределах 40–90%.
* Если влажность **<= порога** (например 60%) → начинается *эмуляция полива*:

  * `isWatering = true`
  * влажность постепенно растёт до диапазона оптимальности (70–80)
  * после достижения 70–80 → `isWatering = false`
  * событие записывается в историю `wateringEvents`

Создай отдельный файл `simulator.js` со всей логикой.

---

## **2. Одностраничный фронтенд (HTML + TailwindCSS + DaisyUI + Chart.js)**

Создай папку `/client`.

Используй:

* **TailwindCSS**
* **DaisyUI**
* **Chart.js (line chart)**

Фронтенд должен:

### 🔹 Реалтайм-данные:

* подключаться к WebSocket серверу
* отображать текущую влажность большим красивым компонентом
* показывать индикатор "Полив включен/выключен" из DaisyUI State Components
* перерисовывать график влажности в реальном времени
* показывать историю поливов за текущий день

### 🔹 Панель настроек (UI):

В отдельной «карточке» сделай настройки управления:

* минимальный порог влажности (по умолчанию 60%)
* оптимальный диапазон (70–80%)
* кнопка «Применить»
* значения должны отправляться на сервер (WebSocket message: `{type:"configUpdate", settings: {...}}`)

Сервер должен подхватывать новые настройки без перезапуска.

---

## **3. Визуальный стиль**

Страница должна быть:

* тёмная тема, аккуратная, современная
* похожа на «monitoring dashboard»
* с большими визуальными блоками
* двумя колонками:
  **слева — данные, справа — график**
* список поливов внизу в виде красивой таблицы DaisyUI

---

## **4. Структура проекта**

```
project/
 ├── server/
 │    ├── server.js
 │    ├── simulator.js
 │    └── package.json
 ├── client/
 │    ├── index.html
 │    ├── main.js
 │    └── styles.css (или CDN Tailwind)
 └── README.md
```

---

## **5. Запуск**

Нужны команды:

### server:

```
cd server
npm install
node server.js
```

### client:

Обычный браузер:

```
open client/index.html
```

(без сборки, просто static HTML)

---

## **6. Что ещё добавить**

### ✔ Таблица событий полива

Колонки:

| Начало | Конец | Длительность | % при старте | % при остановке |

### ✔ Статистика за сегодня:

* средняя влажность
* количество поливов
* длительность всего полива за день
* максимальная влажность
* минимальная влажность

---

## **7. Требования к Claude Code**

Сгенерировать:

* все файлы полностью
* весь HTML/CSS/JS
* весь серверный JS
* все фейковые данные/симулятор
* все взаимодействия WebSocket
* эффект плавной анимации обновления данных
* аккуратный график с Chart.js
* наполненный красивый UI

---