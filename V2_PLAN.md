# План разработки v2: IoT Soil Moisture Monitoring System - Professional Dashboard

## 🎯 Цель версии v2

Создать профессиональный, интерактивный и визуально привлекательный дашборд мониторинга IoT-системы с улучшенным UX/UI, дополнительными датчиками, расширенными функциями управления и 4 различными стилями дизайна.

---

## 📊 Анализ текущего состояния v1

**Текущие проблемы:**
- Простой дизайн без глубокой визуальной иерархии
- Ограниченная анимация (только pulse и fade-in)
- Один датчик (только влажность почвы)
- Использование alert() для уведомлений (плохой UX)
- Нет ручного управления поливом
- Emoji используются вместо иконок
- Нет переключения тем/стилей
- Таблица событий занимает много места
- Настройки всегда видны (должны быть скрыты)
- Нет визуализации порогов на графике

---

## 🎨 Архитектура v2

### Структура проекта

```
client/
├── index.html                    # Главная страница с переключателем стилей
├── styles/
│   ├── minimal.html             # Минималистичный стиль
│   ├── neomorphism.html         # Неоморфизм стиль
│   ├── glassmorphism.html       # Glassmorphism Pro стиль
│   └── cyberpunk.html           # Киберпанк/Футуристичный стиль
├── js/
│   ├── main.js                  # Общая логика (рефакторинг v1)
│   ├── websocket.js             # WebSocket менеджер
│   ├── chart-manager.js         # Управление графиками
│   ├── sensor-manager.js        # Управление данными датчиков
│   ├── notification-system.js   # Система визуальных уведомлений
│   ├── profile-manager.js       # Управление профилями растений
│   └── scheduler.js             # Планировщик полива
├── css/
│   ├── common.css               # Общие стили
│   ├── minimal.css              # Стили минималистичного дизайна
│   ├── neomorphism.css          # Стили неоморфизма
│   ├── glassmorphism.css        # Стили glassmorphism
│   └── cyberpunk.css            # Стили киберпанк
└── assets/
    ├── sounds/                  # (опционально для будущего)
    └── icons/                   # SVG иконки

server/
├── server.js                     # Обновленный WebSocket сервер
├── simulator.js                  # Расширенный симулятор (5 датчиков)
├── profile-storage.js            # Хранение профилей растений
└── scheduler.js                  # Планировщик на сервере
```

---

## 🚀 Основные улучшения

### 1. Новые датчики (симуляция)

Добавить в `server/simulator.js`:

**Датчики:**
- **Влажность почвы** (soil moisture) - уже есть
- **Температура воздуха** (temperature) - 18-30°C, медленные изменения
- **Влажность воздуха** (humidity) - 30-80%, связана с температурой
- **Уровень освещенности** (light) - 0-100%, зависит от времени суток
- **pH почвы** (pH) - 6.0-7.5, очень медленные изменения

**Логика симуляции:**
- Температура: циклические изменения с имитацией дня/ночи
- Влажность воздуха: обратная корреляция с температурой
- Освещенность: синусоида на основе времени суток
- pH: случайные микроколебания ±0.1

**Формат данных WebSocket:**
```json
{
  "timestamp": "...",
  "sensors": {
    "soilMoisture": 65.4,
    "temperature": 24.5,
    "airHumidity": 55.2,
    "light": 78.3,
    "pH": 6.8
  },
  "isWatering": false,
  "wateringEvents": [...],
  "config": {...}
}
```

---

### 2. Библиотеки анимаций и UI

**Добавить через CDN:**

1. **Animate.css** - готовые CSS анимации
   ```html
   <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
   ```

2. **AOS (Animate On Scroll)** - анимация при скролле
   ```html
   <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
   <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
   ```

3. **CountUp.js** - анимация чисел
   ```html
   <script src="https://cdnjs.cloudflare.com/ajax/libs/countup.js/2.6.2/countUp.umd.min.js"></script>
   ```

4. **Lottie** (опционально) - векторные анимации
   ```html
   <script src="https://cdnjs.cloudflare.com/ajax/libs/lottie-web/5.12.2/lottie.min.js"></script>
   ```

5. **ApexCharts** - замена Chart.js (более современный)
   ```html
   <script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>
   ```

6. **Feather Icons** - красивые SVG иконки вместо emoji
   ```html
   <script src="https://unpkg.com/feather-icons"></script>
   ```

---

### 3. Система визуальных уведомлений

**Создать `notification-system.js`:**

**Типы индикаторов:**
- **Граничные значения** - цветная подсветка карточки датчика
- **Пульсирующая рамка** - для критических значений
- **Анимированные иконки** - shake/bounce при проблеме
- **Цветовая индикация статус-бара** - вверху экрана
- **Badge счетчики** - количество предупреждений

**Триггеры:**
- Влажность почвы < минимального порога
- Влажность почвы > максимального порога
- Температура вне диапазона (< 15°C или > 35°C)
- pH вне оптимального диапазона (< 6.0 или > 7.5)
- Потеря соединения с сервером
- Полив активен более 5 минут подряд

**Реализация:**
```javascript
class NotificationSystem {
  notify(type, sensor, value) {
    // Подсветка карточки датчика
    // Анимация shake через Animate.css
    // Добавление badge с количеством предупреждений
    // Изменение цвета статус-бара
  }
}
```

---

### 4. Ручной режим полива

**UI компоненты:**
- Большая кнопка "Manual Watering" с toggle состоянием
- Индикатор "Auto Mode" / "Manual Mode"
- Таймер ручного полива
- Кнопка "Stop Watering" (отмена ручного полива)

**Логика:**
- Когда включен ручной режим, автополив отключается
- WebSocket сообщение: `{type: "manualWatering", action: "start"/"stop"}`
- Сервер должен обрабатывать команды и отправлять обновления
- Ручной полив не останавливается автоматически (только по команде)

**Обновить `server/server.js`:**
```javascript
ws.on('message', (message) => {
  const data = JSON.parse(message);

  if (data.type === 'manualWatering') {
    if (data.action === 'start') {
      simulator.startManualWatering();
    } else {
      simulator.stopManualWatering();
    }
  }
});
```

---

### 5. Планировщик полива

**UI компонент (модальное окно или sidebar):**
- Таблица расписания с временными слотами
- Кнопки "Add Schedule", "Edit", "Delete"
- Выбор дней недели (чекбоксы)
- Время начала и длительность полива
- Toggle "Enable Scheduler"

**Формат расписания:**
```json
{
  "schedules": [
    {
      "id": "1",
      "days": ["monday", "wednesday", "friday"],
      "time": "07:00",
      "duration": 300,  // секунды
      "enabled": true
    }
  ]
}
```

**Логика:**
- Сохранение расписания на сервере
- Проверка текущего времени каждую минуту
- Автоматический запуск полива по расписанию
- Приоритет: ручной режим > расписание > автоматика

**Добавить `server/scheduler.js`:**
```javascript
class WateringScheduler {
  checkSchedule() {
    // Проверка текущего времени
    // Запуск полива если совпадает
  }
}
```

---

### 6. Профили растений

**UI компонент:**
- Dropdown меню выбора профиля
- Кнопки "Create Profile", "Edit", "Delete"
- Модальное окно редактирования:
  - Название профиля
  - Иконка/цвет
  - Пороги для каждого датчика
  - Расписание полива

**Предустановленные профили:**
1. **Суккулент** - низкая влажность (40-50%), редкий полив
2. **Тропическое растение** - высокая влажность (60-80%), частый полив
3. **Овощи** - средняя влажность (55-70%), регулярный полив
4. **Custom** - пользовательские настройки

**Формат профиля:**
```json
{
  "profiles": [
    {
      "id": "succulent",
      "name": "Суккулент",
      "icon": "🌵",
      "thresholds": {
        "soilMoisture": { "min": 40, "optimal": [45, 50] },
        "temperature": { "min": 18, "max": 30 },
        "pH": { "min": 6.0, "max": 7.0 }
      },
      "schedule": [...]
    }
  ]
}
```

**Хранение:**
- LocalStorage для клиента
- Сервер сохраняет в JSON файле `profiles.json`
- Синхронизация между клиентами через WebSocket

**Создать `server/profile-storage.js`:**
```javascript
class ProfileStorage {
  saveProfile(profile) { /* ... */ }
  loadProfile(id) { /* ... */ }
  deleteProfile(id) { /* ... */ }
}
```

---

### 7. Улучшенная визуализация графиков

**Замена Chart.js на ApexCharts:**

**Преимущества ApexCharts:**
- Лучшая производительность
- Встроенный zoom/pan
- Annotations (линии порогов)
- Более современный вид
- Лучшая анимация

**Мультидатчиковый график:**
- Переключатель между датчиками (табы или dropdown)
- Возможность отображения нескольких графиков одновременно
- Визуализация порогов (зоны: красная, желтая, зеленая)
- Маркеры событий полива на временной шкале

**Конфигурация:**
```javascript
{
  chart: {
    type: 'area',
    animations: {
      enabled: true,
      easing: 'easeinout',
      speed: 800
    },
    toolbar: {
      show: true,
      tools: {
        zoom: true,
        pan: true,
        download: true
      }
    }
  },
  annotations: {
    yaxis: [
      {
        y: 60,
        borderColor: '#ff4444',
        label: { text: 'Min Threshold' }
      },
      {
        y: 70, y2: 80,
        fillColor: '#22c55e',
        opacity: 0.1,
        label: { text: 'Optimal' }
      }
    ]
  }
}
```

---

### 8. Четыре стиля дизайна

#### **A. Minimal (Минималистичный)**

**Цветовая палитра:**
- Фон: `#ffffff` (светлый) / `#1a1a1a` (темный)
- Акцент: `#2563eb` (синий)
- Текст: `#0f172a` / `#f1f5f9`
- Границы: `#e2e8f0` / `#334155`

**Особенности:**
- Плоский дизайн без градиентов
- Минимум теней
- Большие пробелы (whitespace)
- Sans-serif шрифты (Inter, SF Pro)
- Иконки вместо emoji
- Простые карточки с тонкими границами

**Анимации:**
- Плавные fade (300ms)
- Subtle hover effects (scale: 1.02)
- Smooth transitions на всех элементах

#### **B. Neomorphism (Неоморфизм)**

**Цветовая палитра:**
- Фон: `#e0e5ec` (светлый) / `#2d3748` (темный)
- Акцент: `#4f46e5` (индиго)
- Тени: мягкие двусторонние тени

**Особенности:**
- Выдавленные/вдавленные элементы
- Мягкие тени: `8px 8px 16px rgba(0,0,0,0.2), -8px -8px 16px rgba(255,255,255,0.7)`
- Закругленные углы (border-radius: 20px)
- Монохромная цветовая схема
- Эффекты вдавливания для активных кнопок

**Анимации:**
- Пульсирующие тени
- Smooth transitions между вдавленным/выпуклым состоянием
- Ripple effect на кнопках

#### **C. Glassmorphism Pro (Стекло)**

**Цветовая палитра:**
- Фон: градиенты `linear-gradient(135deg, #667eea 0%, #764ba2 100%)`
- Карточки: `rgba(255, 255, 255, 0.1)`
- Backdrop: `blur(20px)`
- Акценты: градиентные цвета

**Особенности:**
- Полупрозрачные карточки
- Сильное размытие фона (backdrop-filter: blur(20px))
- Градиентные границы
- Тонкие светящиеся линии
- Layered depth (несколько слоев стекла)

**Анимации:**
- Blur transition
- Opacity waves
- Floating animation (медленное покачивание)
- Gradient animation на фоне

#### **D. Cyberpunk/Futuristic (Киберпанк)**

**Цветовая палитра:**
- Фон: `#0a0e27` (очень темный синий)
- Акценты: `#00ffff` (cyan), `#ff00ff` (magenta), `#ffff00` (yellow)
- Неоновое свечение
- Острые углы (clip-path)

**Особенности:**
- Угловатые формы (clip-path: polygon)
- Неоновые свечения (text-shadow, box-shadow)
- Сетка/grid pattern на фоне
- Glitch эффекты
- Анимированные границы (border-image с градиентом)
- Монопространственный шрифт для данных (Fira Code, JetBrains Mono)

**Анимации:**
- Neon pulse (мигание неона)
- Glitch effect на заголовках
- Animated gradient borders
- Scan line animation
- Flicker effects

---

## 📝 План реализации

### Этап 1: Расширение серверной части

**Файлы для изменения:**
- `server/simulator.js`
- `server/server.js`

**Новые файлы:**
- `server/profile-storage.js`
- `server/scheduler.js`

**Задачи:**
1. Добавить симуляцию 4 новых датчиков в `simulator.js`
2. Обновить формат WebSocket сообщений
3. Добавить обработку команд ручного полива
4. Реализовать систему профилей с сохранением
5. Создать планировщик полива с проверкой расписания
6. Добавить endpoints для управления профилями и расписанием

### Этап 2: Рефакторинг клиентской архитектуры

**Создать модульную структуру:**

**`js/websocket.js`** - управление WebSocket соединением:
```javascript
class WebSocketManager {
  constructor(url) { /* ... */ }
  connect() { /* ... */ }
  send(data) { /* ... */ }
  on(event, callback) { /* ... */ }
  reconnect() { /* ... */ }
}
```

**`js/sensor-manager.js`** - управление данными датчиков:
```javascript
class SensorManager {
  constructor() { this.sensors = {}; }
  updateSensor(name, value) { /* ... */ }
  getSensor(name) { /* ... */ }
  getHistory(name) { /* ... */ }
}
```

**`js/chart-manager.js`** - управление графиками:
```javascript
class ChartManager {
  constructor(containerId) { /* ... */ }
  initChart(type, options) { /* ... */ }
  updateChart(data) { /* ... */ }
  addAnnotation(config) { /* ... */ }
}
```

**`js/notification-system.js`** - визуальные уведомления:
```javascript
class NotificationSystem {
  notify(type, message, sensor) { /* ... */ }
  clearNotifications() { /* ... */ }
  highlightSensor(sensorId, level) { /* ... */ }
}
```

**`js/profile-manager.js`** - управление профилями:
```javascript
class ProfileManager {
  loadProfiles() { /* ... */ }
  saveProfile(profile) { /* ... */ }
  switchProfile(id) { /* ... */ }
  deleteProfile(id) { /* ... */ }
}
```

**`js/scheduler.js`** - клиентский планировщик:
```javascript
class SchedulerUI {
  renderSchedule(schedules) { /* ... */ }
  addSchedule(schedule) { /* ... */ }
  deleteSchedule(id) { /* ... */ }
}
```

### Этап 3: Создание базового HTML шаблона

**Обновить `client/index.html`:**

**Структура:**
```html
<body>
  <!-- Переключатель стилей (фиксированный) -->
  <div class="style-switcher">
    <button data-style="minimal">Minimal</button>
    <button data-style="neomorphism">Neomorphism</button>
    <button data-style="glassmorphism">Glassmorphism</button>
    <button data-style="cyberpunk">Cyberpunk</button>
  </div>

  <!-- Статус-бар с индикаторами -->
  <div class="status-bar">
    <div class="connection-status"></div>
    <div class="alert-badges"></div>
    <div class="mode-indicator"></div>
  </div>

  <!-- Главный контейнер -->
  <div class="container">
    <!-- Хедер с профилями -->
    <header>
      <h1>IoT Plant Monitor</h1>
      <div class="profile-selector"></div>
    </header>

    <!-- Сетка датчиков -->
    <div class="sensors-grid">
      <div class="sensor-card" data-sensor="soilMoisture"></div>
      <div class="sensor-card" data-sensor="temperature"></div>
      <div class="sensor-card" data-sensor="airHumidity"></div>
      <div class="sensor-card" data-sensor="light"></div>
      <div class="sensor-card" data-sensor="pH"></div>
    </div>

    <!-- Панель управления -->
    <div class="control-panel">
      <button class="manual-watering-btn"></button>
      <button class="settings-btn"></button>
      <button class="scheduler-btn"></button>
    </div>

    <!-- Графики -->
    <div class="charts-container">
      <div class="chart-tabs"></div>
      <div id="mainChart"></div>
    </div>

    <!-- Статистика -->
    <div class="statistics-grid"></div>

    <!-- Таблица событий (collapsible) -->
    <details class="events-section">
      <summary>Watering Events History</summary>
      <table class="events-table"></table>
    </details>
  </div>

  <!-- Модальные окна -->
  <div class="modal" id="settingsModal"></div>
  <div class="modal" id="schedulerModal"></div>
  <div class="modal" id="profileModal"></div>
</body>
```

### Этап 4: Создание 4 отдельных HTML файлов со стилями

**Создать:**
- `client/styles/minimal.html` - копия index.html с подключением `minimal.css`
- `client/styles/neomorphism.html` - с подключением `neomorphism.css`
- `client/styles/glassmorphism.html` - с подключением `glassmorphism.css`
- `client/styles/cyberpunk.html` - с подключением `cyberpunk.css`

**Или создать динамический переключатель:**
```javascript
// В index.html
function switchStyle(styleName) {
  const link = document.getElementById('theme-style');
  link.href = `css/${styleName}.css`;
  localStorage.setItem('selectedStyle', styleName);
}
```

### Этап 5: Стилизация каждого варианта

**Создать CSS файлы:**
- `client/css/common.css` - общие стили
- `client/css/minimal.css`
- `client/css/neomorphism.css`
- `client/css/glassmorphism.css`
- `client/css/cyberpunk.css`

**Каждый файл должен содержать:**
- Цветовую схему (CSS custom properties)
- Стили карточек датчиков
- Стили кнопок и модальных окон
- Анимации (keyframes)
- Адаптивные breakpoints

### Этап 6: Интеграция анимаций

**Использование Animate.css:**
```html
<div class="sensor-card animate__animated animate__fadeIn">
```

**Использование AOS:**
```html
<div class="chart-container" data-aos="fade-up" data-aos-duration="800">
```

**CountUp.js для чисел:**
```javascript
const countUp = new CountUp('sensor-value', targetValue, {
  duration: 1,
  useEasing: true
});
countUp.start();
```

**Feather Icons:**
```javascript
// После загрузки DOM
feather.replace();
```

### Этап 7: Тестирование и полировка

1. Проверить работу всех 4 стилей
2. Тестировать на мобильных устройствах
3. Проверить производительность анимаций
4. Оптимизировать update frequency
5. Добавить плавные transitions для всех состояний
6. Проверить работу профилей и планировщика

---

## 🎯 Критически важные файлы

### Серверная часть:
- `/Users/devmansurov/Documents/Development/Neksigol/realtime-app/server/simulator.js` - расширение датчиков
- `/Users/devmansurov/Documents/Development/Neksigol/realtime-app/server/server.js` - обработка новых команд

### Клиентская часть:
- `/Users/devmansurov/Documents/Development/Neksigol/realtime-app/client/index.html` - главный файл
- `/Users/devmansurov/Documents/Development/Neksigol/realtime-app/client/main.js` - рефакторинг в модули

### Новые файлы:
- `client/js/websocket.js`
- `client/js/sensor-manager.js`
- `client/js/chart-manager.js`
- `client/js/notification-system.js`
- `client/js/profile-manager.js`
- `client/js/scheduler.js`
- `client/css/minimal.css`
- `client/css/neomorphism.css`
- `client/css/glassmorphism.css`
- `client/css/cyberpunk.css`
- `server/profile-storage.js`
- `server/scheduler.js`

---

## ✅ Чеклист реализации

**Серверная часть:**
- [ ] Добавить симуляцию температуры воздуха
- [ ] Добавить симуляцию влажности воздуха
- [ ] Добавить симуляцию освещенности
- [ ] Добавить симуляцию pH почвы
- [ ] Реализовать ручной режим полива
- [ ] Создать систему профилей
- [ ] Создать планировщик полива
- [ ] Обновить WebSocket протокол

**Клиентская часть - Архитектура:**
- [ ] Создать WebSocketManager
- [ ] Создать SensorManager
- [ ] Создать ChartManager (ApexCharts)
- [ ] Создать NotificationSystem
- [ ] Создать ProfileManager
- [ ] Создать SchedulerUI

**Клиентская часть - UI:**
- [ ] Создать сетку из 5 карточек датчиков
- [ ] Добавить кнопку ручного полива
- [ ] Создать модальное окно настроек
- [ ] Создать модальное окно планировщика
- [ ] Создать dropdown профилей
- [ ] Интегрировать Feather Icons
- [ ] Добавить статус-бар с индикаторами

**Стили:**
- [ ] Создать common.css
- [ ] Создать minimal.css (минималистичный)
- [ ] Создать neomorphism.css (неоморфизм)
- [ ] Создать glassmorphism.css (стекло)
- [ ] Создать cyberpunk.css (киберпанк)
- [ ] Создать переключатель стилей

**Анимации:**
- [ ] Интегрировать Animate.css
- [ ] Интегрировать AOS
- [ ] Добавить CountUp.js для чисел
- [ ] Создать custom animations для каждого стиля
- [ ] Добавить loading skeletons
- [ ] Добавить smooth transitions

**Графики:**
- [ ] Заменить Chart.js на ApexCharts
- [ ] Добавить визуализацию порогов
- [ ] Создать мультидатчиковый график
- [ ] Добавить zoom/pan функциональность
- [ ] Добавить маркеры событий полива

**Тестирование:**
- [ ] Тестировать все 4 стиля
- [ ] Проверить адаптивность (mobile/tablet/desktop)
- [ ] Протестировать все функции управления
- [ ] Оптимизировать производительность
- [ ] Проверить работу профилей
- [ ] Проверить планировщик

---

## 🎨 Дополнительные улучшения

**Будущие возможности (не обязательно для v2):**
- Экспорт данных в CSV/JSON
- PWA поддержка (оффлайн режим)
- Мультиязычность (i18n)
- Темная/светлая тема для каждого стиля
- Виджеты для drag-and-drop
- Сравнение данных между датами
- Machine Learning предсказания

---

## 📊 Ожидаемый результат

**v2 будет включать:**
✅ 5 датчиков вместо 1
✅ Ручное управление поливом
✅ Планировщик автополива
✅ Система профилей растений
✅ 4 уникальных стиля дизайна
✅ Визуальные уведомления
✅ Современные анимации (Animate.css, AOS)
✅ Улучшенные графики (ApexCharts)
✅ Модульная архитектура кода
✅ Красивые иконки (Feather Icons)
✅ Адаптивный дизайн для всех экранов
✅ Плавные transitions и micro-interactions

**Улучшение по сравнению с v1:**
- 🚀 В 5 раз больше данных (5 датчиков)
- 🎨 В 4 раза больше визуальных вариантов
- ⚡ Лучшая производительность (модульный код)
- 🎭 Богаче анимации (3 новые библиотеки)
- 🎮 Больше контроля (ручной режим + планировщик)
- 📱 Лучший мобильный опыт
